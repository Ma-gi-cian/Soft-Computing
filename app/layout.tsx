import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Soft Computing - Hybrid Ant Colony Optimization and Swarm Particle Optimization",
  description: "A hybrid approach to optimization by combining Ant Colony Optimization and Swarm Particle Optimization for faster and efficient convergence.",
  authors :{name: "Aditya Jha", url: "aditya01jha.02@gmail.com"}
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
